# NEVER commit real API keys here.
# Create this secret manually on your cluster:
#
#   kubectl create secret generic fraud-agent-secrets \
#     --from-literal=GROQ_API_KEY=your_actual_key_here
#
# This file is a template showing the structure only.
apiVersion: v1
kind: Secret
metadata:
  name: fraud-agent-secrets
type: Opaque
stringData:
  GROQ_API_KEY: "REPLACE_ME"
